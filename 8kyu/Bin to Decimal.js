// Complete the function which converts a binary number (given as a string) to a decimal number.

//MY SOLUCTION

function binToDec(bin){
    return parseInt(bin ,2)
  }

    //   parseInt() is a JavaScript function that converts a string to an integer.
    //   It takes two arguments: the first argument is the string to be converted, and 
    //   the second argument (optional) is the radix (or base) of the number system to be used.